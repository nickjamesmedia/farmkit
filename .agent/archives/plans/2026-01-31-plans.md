#.agent/PLANS.md

# Plans

## Active plan: Nav context for shared profile + farm data
**Status:** Completed  
**Owner:** Codex  
**Started:** 2026-01-19
**Completed:** 2026-01-19

### Goal
Stop the nav from flashing placeholder text between route changes by loading
profile + farm info once and sharing it across pages.

### Non-goals
- Redesigning the nav UI or adding new nav links.
- Implementing localStorage/sessionStorage caching (option 2).
- Changing RLS policies or the farm data model.

### Assumptions
- React context can own the shared nav data and expose loading/error state.
- `fetchActiveFarmContext` remains the source for active farm selection.
- Parent farm lookup stays in the nav data loader.

### Work items (ordered)
1) [x] Create a nav data context/provider that loads profile + farm data once.
2) [x] Wrap the app routes with the provider in `frontend/src/App.tsx`.
3) [x] Refactor `frontend/src/components/Nav.tsx` to read from the context.
4) [x] Confirm search uses the shared `activeFarmId` from context.
5) [x] Add a minimal loading state to avoid placeholder flashes.

### Acceptance criteria
- Navigating between pages does not flash “Set farm name” in the nav.
- Nav shows parent farm name and branding consistently after load.
- Only one nav data fetch occurs per session (no per-route reload).

### Validation
- Manual navigation across multiple routes; verify nav text is stable.
- Confirm no console errors during nav render or search.
- Not run (manual check pending).

---

## Backlog plans (inactive)
_Add new plans here when needed. Keep only one Active plan at a time._
